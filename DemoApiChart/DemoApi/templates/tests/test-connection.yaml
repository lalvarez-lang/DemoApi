apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "DemoApi.fullname" . }}-test-connection"
  labels:
    {{- include "DemoApi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "DemoApi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
